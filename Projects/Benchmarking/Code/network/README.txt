
The Network benchmark generates the throughput and latency for a TCP and UDP
connection. The directory contains 4 programs - 2 for UDP (client + server)
and 2 for TCP (client + server). The results of iperf is available in
iperf_results.txt.

====
Instructions to run the programs :

1. Go to directory – network

2. To run the program, run command: ./script.sh

3. This will run both the UDP and TCP programs. 
